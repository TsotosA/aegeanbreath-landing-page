{
  "name": "aegeanbreath-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page for Aegean Breath. Tailwind compiled to styles.css.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
